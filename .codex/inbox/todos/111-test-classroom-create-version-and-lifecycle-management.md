---
type: todo
id: T-111
title: "补齐多测试课堂的新建入口、版本选择与归档删除管理"
status: completed
created: 2026-09-10
updated: 2026-09-11
captured_by: project-inbox
priority: P1
priority_basis: user-reported-test-workflow-blocker
related: [T-085, T-094, T-097, T-101, T-102, T-108, T-109, T-110]
tags: [todo, test-classroom, lifecycle, version, navigation, studio]
---

# 补齐多测试课堂的新建入口、版本选择与归档删除管理

## 用户反馈

课堂中心只看到一场 r9 测试课，用户希望测试新保存的 r12，却找不到创建入口；旧测试课堂也没有删除入口。

### 2026-09-11 再次确认：CRUD 中的 D 必须交付

> 还有历史的 TEST UI 验收，我提出过需要能够 CRUD，CRU 是有了，D 呢？

用户提供的截图仍只有“进入我的课堂”，没有删除操作。**删除不是新需求，也不是可选项；本单此前将其写成“方案待确认／若获准实施”，弱化了用户已提出的要求，现予纠正。** CRU 已有是用户反馈，本次没有据此推定其全部边界已验收。重开审计时 D 尚未交付；该历史状态现已由下文的真实删除实现、隔离测试与生产发布取代，且从未用新增版本标签、重置或归档冒充 CRUD 的 D。

重开时的源码复核（实施前证据）：`ClassroomHub.tsx` 的 `RoomCard` 当时只有进入课堂链接；`app/api/platform/classrooms/[classroomId]/route.ts` 当时只有 GET，现行平台 classroom 路由未发现删除端点或等价删除动作。`resetClassroomInstance` 一类流程中的 DELETE 明细 SQL 是重置运行数据，不是删除课堂实例。旧 API 的 archive-room 也不能证明现行 TEST 的 D 已实现。该段用于保留缺口根因，不代表当前实现状态。

## 源码核对

- `createClassroomInstance()` 每次生成独立的 roomId、teamId、factorySeed；未发现只能创建一场 TEST 的全局限制。列表只显示一场不能证明系统限额为一场。
- `ClassroomHub.tsx` 的 `FactoryPanel` 位于 TEST/PRODUCTION 列表之后，仅在有 Studio 权限且 bootstrap 加载成功时出现。
- 初始加载先取课堂列表，再并行取完整 Studio bootstrap 和账号；`setBootstrap` 要等两者都成功。任一依赖失败可能出现“有旧课堂列表，但无创建表单”，页面只有顶部通用错误，缺少就地原因与重试入口。
- 没有在 TEST 区标题旁放显眼的新建入口。视图回执缺失时当前版本还会被 eligible 过滤，关联 T-108。
- 当前 `RoomCard` 只提供进入课堂，当前 `/api/platform/classrooms` 为列表/创建；本轮未发现当前平台提供课堂删除/归档的完整 UI/API 链路。
- 旧 `ClassroomApp.tsx` 存在 legacy archive-room 操作，不等于现行 ClassroomInstance 流程已经有可用的归档入口，不能直接套用后宣称完成。

本轮依据截图和源码记录；未读取该用户线上完整响应，不能断言入口消失具体由哪一个请求或权限条件导致。

## 目标行为

### 1. 多场测试独立存在

- 同一课程允许有 r9、r12 等不同版本的测试课堂，也允许同一 exact 版本创建多场，用于不同人数、账号组合和发牌 seed 的测试。
- 每场显式展示 classroomId、courseId/revision/digest、人数、运行状态和更新时间；默认名称可带课程版本，避免都叫“测试课堂”无法分辨。
- r9 旧课堂始终锁定 r9，不因编辑器保存 r12 或点击“重置”而被静默升级。
- 新版本通过自己的有效视图验收后新建 TEST；r9 的回执不能冒充 r12 的回执。

### 2. 新建与版本入口始终可发现

- TEST 标题/列表顶部提供“新建测试课堂”，不要求用户先滚过全部课堂或找页面底部的工厂。
- 该入口按权限显示；缺课程/回执/账号等条件时显示具体原因与下一步，而不是整个创建区消失。
- 流程明确为：选择课程与 exact 版本 → 完成该版本视图验收（如缺）→ 配置人数/成员/课件 → 创建独立 TEST。
- r12 待验收时也能看到 r12 和“去验收 r12”；不回退到 r9，也不误导用户删除 r9 才能测试 r12。
- 从发布页的 r12 跳入创建时，完整保留 r12 courseId/revision/digest 与视图回执；响应迟到或指针变化时明确提示，不静默换版本。
- 课程、账号或课堂列表加载失败要分别反馈、可分别重试；保留已经显示的课堂和已填表单。
- 与 T-109 对齐：长期入口归于 Studio 的内部测试/课程管理，不再让普通学员的正式课堂入口承担测试管理。

### 3. 旧测试课堂的删除（必交付）与独立归档

- 在有管理权限的 TEST 卡片或其清晰可达的管理菜单中提供“删除测试课堂”，历史/已归档 TEST 也应有对应管理入口；不能只有进入课堂。
- 点击删除先显示实例名称、classroomId、环境、exact 课程版本、将删除的数据范围、保留项及依赖检查结果，再二次确认；用户取消不产生写入。按钮禁用或请求被拒绝时必须说明具体原因。
- 无阻断依赖的 TEST 可真正完成删除：实例退出活跃及普通历史列表，旧链接不再进入可运行课堂，刷新后不会恢复。存储采用物理删除还是保留最小审计墓碑由实现设计明确，不得把只改成 archived 当作删除完成。
- 归档可作为独立、非破坏性操作：从活跃列表移出，保留深层“已归档”入口、版本、进度及历史证据，归档后只读；它不能取代删除按钮或删除验收。
- 区分“重置同版本重新测”“归档不再使用”“永久删除测试数据”，不能三个按钮都只是清空进度。
- 永久删除属于破坏性动作；明确授权、二次确认和影响预览，不因本条反馈直接执行删除。
- 归档/删除只作用于指定 TEST 实例及明确属于它的数据；不删除全局账号、课件包、课程版本、其他团队 Membership 或正式课堂。
- 已关联 UI 回执、Released、正式课堂或不可变审计证据的 TEST，必须先判定依赖。此类依赖可以阻止物理清除，但不能因此省略全部 TEST 的删除能力：明确展示阻断对象及原因，并提供保留证据的处理路径（如独立归档）。最终保留/删除策略纳入设计和测试，不让既有发布证据悬空或被悄悄改写。
- 明确归档后回执是否继续可用于新的发布/开课的政策；历史证据保留与当前可用性分开建模，不能不经设计就统一抹掉或认定有效。
- 权限按当前 Room 管理契约设计，不能让“任何导师”删除其他团队课堂，也不能只在前端隐藏按钮。
- 并发运行/归档/删除需服务端原子校验与可审计结果；出现冲突应中止并解释，禁止部分删除。

## 验收

- [x] 同一账号可以找到新建入口，同时保留 r9 旧 TEST 并创建 r12 新 TEST。
- [x] r12 无回执时明确引导验收 r12，有回执返回后继续已填配置，不自动改成 r9。
- [x] 同版本不同人数/成员组合可分别创建 TEST，实例状态与数据互不影响。
- [x] 课程/账号加载失败、无权限、无课程、回执失效均给出相应提示，不只剩旧课堂卡片。
- [x] 普通点击即可打开新建、版本验收、进入课堂和归档列表；纳入 T-110 的 Enter/触摸/Ctrl/Cmd 回归。
- [x] 旧 TEST 有明确的重置/归档管理入口；归档后可从历史列表找到，恢复策略与可写性明示。
- [x] 有权限的用户能在 TEST 卡片及历史管理入口找到删除；无权限、依赖阻断、运行中冲突和网络错误均有明确提示，不是无反应。
- [x] 无依赖 TEST 经二次确认可删除，刷新列表后消失、旧链接不可继续运行；取消删除无写入；重复请求不会误删其他实例。
- [x] 删除完整 UI/API 链路覆盖无依赖/有回执依赖/与正式课堂关联/权限不足/并发冲突等隔离测试，不使用用户真实课堂验证。
- [x] TEST 删除不误删 PRODUCTION；不级联删除共享账号、课程 JSON 版本、导师课件或其他课堂数据；保留所需审计证据。
- [x] CRUD 分别列出实现、自动化测试、部署与真实 UI 机器验收证据；D 的真实物理删除使用隔离临时 D1 验证，归档/重置未计作 D；用户人工 View/UI 验收仍单列、未代签。
- [x] 不改写 r9 课程数据，不复用其回执冒充 r12 验收，不删除参与多个课堂的共享账号。

T-108 负责创建门禁可操作性，T-110 负责普通点击与加载性能，本单负责多测试实例入口及其生命周期管理。物理删除已完成实现、隔离测试和生产发布；不会用用户真实课堂做破坏性冒烟。


## 2026-09-11 删除本地实施证据

- TEST 活跃卡和历史归档卡均加入明确删除入口；服务端影响预览展示 exact 实例、删除范围、保留项和阻断 reference IDs，勾选二次确认后才发送 DELETE。
- 新增 `GET ...?deletePreview=1` 与 `DELETE /api/platform/classrooms/{id}`；只允许真实登录的本课堂 Admin DM，测试身份、学员、普通导师和 Production 失败关闭。
- 新增不可变最小墓碑、证据依赖触发器、room 删除触发器和原子 CAS 删除。无依赖 TEST 被实际物理删除，旧链接 410；有 UI/历史回执或发布依赖时拒绝。
- 单元/数据库故障注入和编译后浏览器 + 临时 D1 覆盖活跃及历史真实删除、取消、断网、权限、依赖和 Pad。证据见 `docs/TODO_111_IMPLEMENTATION.md` 与 `docs/qa/t111-test-classrooms/`。
- 自动化没有删除生产站点上的任何课堂，也没有代签人工 View/UI 验收；生产发布和非破坏性复验现已由下节回执补齐。

## 2026-09-11 生产部署与非破坏性复验

- 最终生产 release：`20260911T192157CST-t093-t111-r2`。
- 源提交：`256ac17d8b4c2fb6757e8b9408eaffcb929ca417`；App Build ID：`08d0a9e1-9a5e-49ae-9389-57eeecc0df59`。
- Hecate 完整 healthcheck 与 public smoke 通过；生产 D1 已出现 `classroom_deletions` 表及 6 个删除/证据/不可变性 guard trigger。
- 部署前后均保留原有 1 场 TEST；生产墓碑仍为 0。即发布没有删除任何真实课堂，真实物理删除只在隔离临时 D1 中完成。
- 匿名访问影响预览与 DELETE 均返回 `401 AUTH_REQUIRED`；登录但并非目标课堂 Admin DM 的导师看不到删除入口。Production 禁止、依赖阻断、并发/故障回滚及真实活跃/归档 TEST 删除由单元、故障注入和编译后浏览器专项覆盖。
- 本单工程交付完成不等于用户已签署人工 ViewAcceptanceReceipt / UiAcceptanceReceipt；该人工动作仍明确保留。
