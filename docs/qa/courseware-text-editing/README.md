# 2026-09-21 课件文字编辑验证

本地源码：同提交。生产部署记录另行追加，不将本地通过等同于已上线。

- TypeScript、ESLint、平台 231/231、部署测试 65/65、构建和应用 Smoke 通过。
- 两课结构／不可变注册／回顾页测试通过。
- 独立弹窗真实浏览器测试通过；真实 API + 隔离 SQLite 保存完整链路见 browser.json。
- 全部 54 页 × 投屏／教师双视图，文本对比度、溢出、页面跳转和互动回归见 layout.json。
- 人工查看390px弹窗截图：标签、正文、按钮均可读，无横向溢出。
- 所有写入只针对本地合成数据，没有修改真实课件文字，也没有代签 View/UI 或教学验收。

## Hecate 发布

- 源码：`8dd9b1b`；发布包：`20260921T2155CST-courseware-text-edit`。
- 前一发布：`20260920T1603CST-course-mentor-groups`；部署前自动备份已完成。
- Hecate 重建、65 项部署测试、课程注册只读预检、部署后健康检查通过。
- 650 个历史课件文件逐字节比对保持不变；新增 P1 r9、P2 r4。
- P1 digest：`a336543585d9b53d3bb1596f066bb4ccd8358d0df9d21152280130624d745f44`。
- P2 digest：`8e160cb6c9b57bf25dd485a591c59fbe5f20e4426f32c358070e326a9590a280`。
- 服务端构建日志：`~/Services/minisv/logs/build-20260921T2155CST-courseware-text-edit.log`。
- 部署日志：`~/Services/minisv/logs/deploy-20260921T2155CST-courseware-text-edit.log`。

### 线上复验发现及修复

首轮包的页面可用，但新 API 在公网返回静态 404，导致按钮禁用；未将此状态报告为完成。原因是生产网关使用显式 API 白名单，本地 Worker 测试不经过 Nginx，不能替代部署路由验证。

修复提交 `30b2f83`：新增专用转发、限流、请求大小边界；增加 2 项部署回归，公共 Smoke 强制验证两课编辑 API 的匿名 401，防止静态 404 再漏过。最终部署包为 `20260921T2205CST-courseware-text-edit-r2`，静态课件 digest 不变，部署测试共 67 项。

最终线上浏览器复验通过，见 live.json：导师 P1/P2 启用文字编辑、标题弹窗打开／取消、服务器读取；不合法 POST 明确 400 且无写入；课件库指向新版；学员教师页 403／投屏页 200；历史页面 200。线上文字仍为原文 v0，没有写入验收样例。

公网全资源哈希扫描曾在 P2 既有透明 Logo 图片下载时超时（已收到 200，响应体未在 45 秒内读完），不记为全量通过。650 个历史文件已在服务器构建阶段完成字节比对；新增编辑关键脚本另行进行公网哈希复核。

关键资源公网复核通过：21 个 HTML／运行时／编辑器 JS／CSS 的哈希与本地构建一致；匿名访问 401、学员教师页 403、导师访问 200，当前及历史导航均通过。见 live-assets.json。
