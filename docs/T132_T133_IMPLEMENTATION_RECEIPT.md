# T-132／T-133 本地实施回执 · 2026-09-19

## 状态

已实际修改源课件、运行构建和真实浏览器测试；当前为 **候选版**，不是线上发布回执，不代签 View/UI 或导师试讲。

- 开发分支：`codex/t132-t133-120min`
- 工作区：`/private/tmp/minisv-t124-1789402205`
- 基线：`f4a517e301395636fa816b5b29a00423257631b3`
- 稳定交付副本：`cowork/课件/development-courseware/p1-p2-120min-review-20260919/`
- 完整包含私有教师提示，不能整体放入公开目录。

## 实现

- P1：25 页／120 分钟；保留原 18 页 ID、三组 Three.js、校园失物招领和订餐、AI 帮手、黑箱测试；补六字段描述、粒度取舍、“马上用到我的游戏”40 分钟活动与三项个人成果。
- P2：27 页／120 分钟；保留旧立棍思想与预约短例；统一六项；完整校园寻宝主棍、六份真实不同的子棍、逐问启动词、返工单；任意新增自己的子棍。
- 两端：投屏与教师提示物理分包；课程 ID／版本／会话隔离；逐步揭示、目录、键盘、刷新恢复、手动计时。
- P1 四张 A4 内容初稿：速填、模块地图、详细设计卡、验证记录；HTML／Markdown 下载与试打印 PDF。
- P2 编辑／复制／当前及合并下载／打印；复制被拒绝时实际选中文本提示手动复制。无云端保存、无新增后台。
- 寻宝实际可玩：键盘和触摸，首版重复计数反例与修正版，同组 7 项检查；不是学生或 AI 的现场生成结果。

## 自动化证据

全部通过：

1. `npm run test:t132-t133:courseware`：两课总时长及六阶段预算、逐页教师字段、旧源单元、完整模板及固定六标题、分包、静态资源链接、可复现 hash、示例程序结果。
2. `python3 courseware/shared/tests/verify-browser.py`：52 页 × 投屏／教师两个表面逐页字体对比度、内容溢出、标题遮挡；真实目录点击、快捷键、手动计时、刷新、双屏同步、跨课隔离、BroadcastChannel 不可用时的 storage 回退。
3. 同一浏览器测试实际点击复制，读取剪贴板比对完整正文；逐份六子棍、编辑切换保留、新建子棍、真实文件下载、复制失败兜底与打印；真实点击游戏和完整获胜路径；390px 触摸页无横溢出。
4. `MSV_T122_URL=http://127.0.0.1:18132/module-thinking-deck/dist MSV_T122_MODE=dist python3 courseware/module-thinking-deck/tests/verify-browser.py`：三景真实 WebGL、鼠标／键盘／触摸、教师投屏同步、刷新与会话隔离、S10 黑箱交互与全页对比度回归。
5. `node --import tsx --test tests/development-mentor-package.test.ts tests/courseware-versioning.test.ts tests/courseware-navigation.test.ts`：19／19；旧课件版本与现有导航权限契约保留。
6. `npm run lint`、`npm run typecheck`、`npm run audit:todos`、`git diff --check` 均通过。TODO 审计保留其他任务既有未完成／人工检查警告，没有顺手改完成。
7. 四张 A4 实际输出 PDF，各 1 页；已查看设计卡、走查卡、P1/P2 投屏、教师提示与工作台截图。

浏览器原始报告：`evidence/report.json`；截图与 PDF 同目录。只使用隔离 localhost 页面与预设数据，未访问或修改生产课堂／学生数据。

修复过程中实际发现并处理：按钮焦点让整页快捷键失效、计时器浅底黄字、手游示范 grid 最小宽度溢出、材料页 favicon 404；没有仅通过改断言掩盖这些缺陷。旧测试中的 18 页／60 分钟、旧图标版本和旧材料标题断言按新已确认规格更新。

## 候选构建身份

- P1 `2026.09.19-p1-r6`：`9113848ba1f5320eefed050ed6b404666f058db7eff32070cd72d2397172d1c7`
- P2 `2026.09.19-p2-r1`：`f94fe313d304ae81605640f14a41d42abd43cbb3b65e6f977fe03bb9cd745c67`

`dist/BUILD-MANIFEST.json` 声明 candidate 和未签人工验收；二次构建 digest 一致。

## 尚未发生

- 未提交或推送 Git，未生产部署，未修改现有 Courseware Library 发布指针。
- 未代签导师 120 分钟现场试讲、真实学生可理解性、真实扩展投影／iPad 或学生自选 AI 工具实操验收。
- 四份材料是内容初稿，不冒称同事的最终实体设计。
- 正式发布需要建立新不可变版本并验证 P2 教师权限及旧版入口；不能把本候选工作区直接塞进旧生产打包流程。本轮不改其校验来强行上线。

## 延续开发

以课件源文件为真值，修改后运行共同构建。时间计划与逐页脚本文档由同一份 slide／note 数据派生；模板浏览器 catalog 由 Markdown 派生。旧讨论移到 `docs/decisions/T132-discussion-history.md` 与 `T133-discussion-history.md`，避免五组与六项、固定休息等过时建议再次覆盖已确认决定。

## 后续：用户授权部署与三维视角修复

用户已要求直接部署，并补充上下拖动、缩放；因此前面的“未部署”段落是首轮候选交付历史，不代表最新发布结果。

已接入匹配本地 Three.js r180 的官方 OrbitControls：水平／垂直旋转、Shift／右键平移、滚轮／双指缩放、复位及键盘控制。真实输入回归发现 S02-C 的舞台本身有 `data-voxel-case`，被通用属性选择器误当按钮，鼠标松手冒泡后重新渲染。两端均改用 button 选择器，从事件绑定根源消除拖动回弹。无需改模型或调整浏览器配置。

发布采用 P1 r5、P2 r1 新 URL，登记新摘要，保留旧 registry 记录与旧包；P2 新教师入口使用与 P1 相同服务端导师权限。生产发布版本和实测证据将在部署完成后追加；不预先宣称成功。
