# T-133｜P2 可视化教学补强

## 范围与版本

- 27 页 / 120 分钟 / 27 页私有教师提示；不增加课时，不扩建后台。
- 六项顺序保持：执行者、上下文与项目设计、目标、约束、避免、验收。
- 继续保留设备预约短例、校园寻宝、全部模块子棍、个人实践、可编辑 Markdown 与复制/下载/打印。
- P2 新版本 `2026.09.19-p2-r2`，独立 `/r2/`；P2 r0/r1 与 P1 r4–r7 不覆盖。
- 维护工作区：`/private/tmp/minisv-t133-visual-r2-20260919`，分支 `codex/t133-visual-r2`。检测到原临时目录有并行三维引擎改动后隔离，不纳入本任务，也未回退他人修改。
- P1 保持 digest `dc678fb345cf938004f223e8a8c9f2faaf636fd481fa675ad3ea219199b70457`。
- P2 digest `2427551404eccf63e594b2a28654d5d06e76e0c6d6fec95b5f612cb69d954446`。

## 五组图的实际位置

1. P2-07：主棍全貌。六项信息卡，完整愿景、本轮范围、共同规则分开标示。
2. P2-11：主棍与六份同级子棍。虚线连接整体分工组，不把模块画成串行流程。六次揭示分别显示负责/不负责。
3. P2-18：背包子棍展开。六项同序，继承用青色标签，本模块职责用蓝色卡头，底部单独显示线索→背包→胜负交接。
4. P2-22：要求→交给 AI→首版→偏差→同输入修正。真实演示新增独立 A/B/B 重放按钮，首版 3、修正版 2，缺 C 不赢。两版本共用相同胜负判定，差别在背包是否正确去重；没有放宽主棍规则。
5. P2-19 / P2-26：运行时数据交接与迁移。检查重复/未到出口/正常三种路径；换成学生自己的模块，不强制每个游戏拆六块。

教师讲稿同步说明每次揭示、追问、误区和操作。图是可编辑 HTML/SVG，核心信息不依赖 Hover。P2 独立 CSS 显式定义浅色底与深色字，避免教师壳继承白字；共享 P1 引擎没有修改。

## 验证方式

- `npm run test:t132-t133:courseware`：课程结构、120 分钟分配、11 个模板、双包隔离、确定性构建、历史身份和可视化语义。
- `python3 courseware/ligun-deck/tests/verify-visual-browser.py`：真实点击/键盘逐步揭示、教师—投屏同步、六张图布局、触摸、真实 A/B/B 重放，不改变当前游戏位置。
- `MSV_DECK_EVIDENCE=... python3 courseware/shared/tests/verify-browser.py`：52 页×2 表面文字对比度/溢出，复制实际编辑文字、下载、打印、游戏、会话隔离。
- `npm run test:course-platform`、typecheck、lint、应用 build/smoke、T-090 E2E 和部署测试。
- 发布时逐字节检查所有既有课件文件；无签署 View/UI 回执、无自动签署人工试讲。

## 人工验收仍待完成

学生能否看图说清主棍/子棍、模块分工、继承与交接、偏差与复查，须由导师实际试讲及学生表现确认。自动化通过不代表以上教学效果已通过。

部署与在线复验记录在执行完成后追加，不预填上线成功。
